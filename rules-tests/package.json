{
  "name": "spark-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests. Runs against the LOCAL emulator only (project demo-spark) \u2014 never touches production.",
  "scripts": {
    "test": "cp ../firestore.rules ./firestore.rules.copy && firebase emulators:exec --only firestore --project demo-spark --config firebase.json \"node test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
